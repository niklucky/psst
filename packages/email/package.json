{
  "name": "@psst/email",
  "version": "0.0.0",
  "private": true,
  "description": "Transactional email sending (Resend) and templates",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo \"No lint yet\""
  },
  "devDependencies": {
    "@psst/config": "workspace:*",
    "typescript": "^5.8.3",
    "vitest": "^4.1.7"
  },
  "dependencies": {
    "resend": "^6.12.4"
  }
}
