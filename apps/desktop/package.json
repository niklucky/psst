{
  "name": "@psst/desktop",
  "version": "0.0.0",
  "private": true,
  "description": "Tauri desktop app (shares web UI)",
  "scripts": {
    "dev": "echo \"Tauri not yet configured\"",
    "build": "echo \"Tauri not yet configured\"",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"No lint yet\"",
    "test": "echo \"No tests yet\""
  },
  "devDependencies": {
    "@psst/config": "workspace:*",
    "typescript": "^5.8.3"
  }
}
